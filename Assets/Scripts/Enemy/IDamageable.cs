// Moth Harper
// This script builds an interface used for damageable gameobjects, like enemies and breakables

using UnityEngine;

public interface IDamageable
{
    public void TakeDamage(float damage);
}
